# group [all] will exist by default
#[all:vars]
#ansible_port=1234
# the specific groups declarations take precendence over the "all" :vars

[local]
control ansible_connection=local

[aws]
# login as remote-ansible and become root
ec1 ansible_become=true ansible_become_pass=remote-ansible # ansible_port=2222
ec[2:3]

[aws:vars]
ansible_user=remote-ansible

[test:children]
aws
local

#[aws]
## login as remote-ansible and become root
#ec1 ansible_user=remote-ansible ansible_become=true ansible_become_pass=remote-ansible # ansible_port=2222
#ec2 ansible_user=remote-ansible
#ec3 ansible_user=remote-ansible

